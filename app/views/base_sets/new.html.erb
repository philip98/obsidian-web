<% provide(:title, "Ausleihe buchen") %>
<h1>Ausleihe buchen</h1>

<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<%= form_tag student_base_sets_path(@person), :method => :post do %>
			<h3>
				<%= @person.name %>
				<small><%= @person.display_class %></small>
			</h3>
			<%= render "shared/isbns" %>
			<%= submit_tag "Ausleihen", :class => "btn btn-primary" %>
		<% end %>
	</div>
</div>